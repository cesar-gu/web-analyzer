// Previously an API endpoint. For static GitHub Pages build, the analysis
// is performed client-side via `pageSpeedService` to avoid requiring a server
// adapter. This placeholder file remains to preserve history; it exports no
// HTTP handler and will not be treated as a server endpoint.

export default {};
